# Barber Platform - Environment Variables Template
# Copy this file to .env and fill in your actual values
# NEVER commit .env files with real secrets to version control

# ============================================
# Spring Profile Configuration
# ============================================
# Options: dev, prod
# dev  = H2 in-memory database, test data, H2 console enabled
# prod = PostgreSQL database, no test data, H2 console disabled
SPRING_PROFILES_ACTIVE=dev

# ============================================
# JWT Configuration (REQUIRED)
# ============================================
# Generate with: openssl rand -base64 64
# Must be at least 256 bits (32 bytes)
JWT_SECRET=REPLACE_WITH_GENERATED_SECRET

# ============================================
# Admin Account (REQUIRED)
# ============================================
# Password requirements:
# - Minimum 12 characters
# - Include: uppercase, lowercase, numbers, special characters
ADMIN_PASSWORD=REPLACE_WITH_STRONG_PASSWORD

# ============================================
# Database Configuration (REQUIRED for Production)
# ============================================
# For development (H2), these can be left empty
# For production (PostgreSQL), all must be set

DB_NAME=barber_db
DB_USER=barber_app_user
DB_PASSWORD=REPLACE_WITH_STRONG_DB_PASSWORD
DB_URL=jdbc:postgresql://localhost:5432/barber_db
DB_DRIVER=org.postgresql.Driver

# ============================================
# Development Test Data (Optional - Dev Only)
# ============================================
# Password for test users in development mode
# If not set, defaults to "TestPass123!"
DEV_TEST_PASSWORD=TestPass123!

# ============================================
# CORS Configuration (REQUIRED)
# ============================================
# Development
CORS_ORIGINS=http://localhost:4200

# Production (update with your actual domain)
# CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# ============================================
# Instructions
# ============================================
# 1. Copy this file: cp .env.example .env
# 2. Generate JWT secret: openssl rand -base64 64
# 3. Set strong admin password
# 4. Update database credentials (for production)
# 5. Update CORS origins
# 6. Verify .env is in .gitignore
# 7. Load variables before starting application
#
# For detailed instructions, see SECURITY-SETUP.md
